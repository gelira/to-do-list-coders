const LIST_PRIORITIES = ['low', 'medium', 'high'];

const PRIORITIES = {
  LOW: LIST_PRIORITIES[0],
  MEDIUM: LIST_PRIORITIES[1],
  HIGH: LIST_PRIORITIES[2],
};

module.exports = {
  LIST_PRIORITIES,
  PRIORITIES,
};
